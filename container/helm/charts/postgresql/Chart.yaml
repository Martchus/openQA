annotations:
  images: |
    - name: postgresql
      image: docker.io/bitnamisecure/postgresql
      tag: latest
  licenses: Apache-2.0
  tanzuCategory: service
version: 0.1.0
apiVersion: v2
appVersion: latest
dependencies:
- name: common
  repository: oci://registry-1.docker.io/bitnamicharts
  tags:
  - bitnami-common
  version: 2.x.x
description: PostgreSQL (Postgres) is an open source object-relational database known
  for reliability and data integrity. ACID-compliant, it supports foreign keys, joins,
  views, triggers and stored procedures.
name: postgresql
